# to be run in the root directory of an existing git repository that has already pushed changes to it's remote counterpart at least once


git init
git add .
git commit -m"update"
git push -u origin main

