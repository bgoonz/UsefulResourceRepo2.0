%h2 Welcome back !

%hr

%h3 Option 1: Choose the service you used when you subscribed

= render 'authentications/providers'

%hr

%h3 Option 2: If you created an account specific to this site

= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f|
  .field
    = f.label :email
    = f.email_field :email
  .field
    = f.label :password
    = f.password_field :password
  - if devise_mapping.rememberable?
    .field
      = f.check_box :remember_me
      = f.label :remember_me
  .actions= f.submit "Sign in"

%hr

= render :partial => "devise/shared/links"
