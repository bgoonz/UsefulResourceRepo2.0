<%= simple_form_for @image, :url => images_path do |f| %>

  <%= f.input :name %>
  <%= f.input :imagefile, :as => :file %>
  <%= f.input :album_id, :collection => @albums %>


  <%= f.button :submit %>

<% end %>
