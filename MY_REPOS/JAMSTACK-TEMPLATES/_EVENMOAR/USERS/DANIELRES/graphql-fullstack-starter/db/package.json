{
  "name": "db",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "up": "yarn dc up -d",
    "kill": "docker-compose kill",
    "rm": "run-s 'dc kill' 'dc rm -f'",
    "dc": "docker-compose"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
