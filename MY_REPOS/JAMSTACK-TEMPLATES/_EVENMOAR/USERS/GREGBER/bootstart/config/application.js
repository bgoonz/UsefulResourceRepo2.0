var express = require('express'),
app = express();

//root
global.base = require('path').resolve(__dirname + '/../');

// db connection
//require('mongoose').connect('localhost', 'test');

module.exports = exports = app;