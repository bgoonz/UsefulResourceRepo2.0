require "spec_helper"

describe Chartable do
  it "has a version number" do
    expect(Chartable::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
