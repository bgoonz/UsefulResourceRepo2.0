export default class Stack<T> {
  // TODO:
  //   - `setEqualityFn(equalityFn?: void | EqualityFn<T>)` - Sets the function used for checking the equality of two elements.
  //   - `size: number` - The number of elements in the stack.
  //   - `get isEmpty(): boolean` - Returns true if there are no elements in the stack.
  //   - `push(element: T): T` - Adds the given element to the top of the stack.
  //   - `pop(): T` - Removes and returns the element at the top of the stack.
  //   - `top(): T` - Returns the element at the top of the stack.
  //   - `toArray(): Array<T>` - Returns an array of all elements in the stack, starting at the top.
  //   - `forEach(fn): void` - Calls `fn(element)` for each element in the stack, starting at the top.
  //   - `contains(element: T, equalityFn: EqualityFn<T>): boolean` - Returns true if the given element is in the stack.
  //   - Anything else?
}
