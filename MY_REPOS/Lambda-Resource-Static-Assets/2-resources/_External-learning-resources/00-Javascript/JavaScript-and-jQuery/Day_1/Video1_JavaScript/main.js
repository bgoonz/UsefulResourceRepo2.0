/**
 This is a block comment, it spans multiple lines
 and won't be executed by the JavaScript engine 
*/

// This is a one line comment

10 + 20 + 10;

// The above lines 9-13 are interpretted by JavaScript as 1 expression
// It's better to write it cleaner though:
10 + 20 + 10;

// Log messages to the console like this:
console.log("Hello World!");

// Note: lines don't have to end with ; but it is wise to do so
console.log("This line ends with ;");
console.log("This line does not, but still runs fine");
