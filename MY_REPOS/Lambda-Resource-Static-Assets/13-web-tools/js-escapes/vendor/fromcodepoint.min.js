/*
 https://lambda-w-1-notes.netlify.app/13-web-tools/fromcodepoint v0.2.1 by @bgoonz */
"use strict";
String.fromCodePoint ||
  (function () {
    var f = (function () {
        try {
          var d = {},
            b = Object.defineProperty,
            c = b(d, d, d) && b;
        } catch (e) {}
        return c;
      })(),
      k = String.fromCharCode,
      l = Math.floor,
      h = function (d) {
        var b = [],
          c = -1,
          e = arguments.length;
        if (!e) return "";
        for (var g = ""; ++c < e; ) {
          var a = Number(arguments[c]);
          if (!isFinite(a) || 0 > a || 1114111 < a || l(a) != a)
            throw RangeError("Invalid code point: " + a);
          if (65535 >= a) b.push(a);
          else {
            a -= 65536;
            var m = (a >> 10) + 55296;
            a = (a % 1024) + 56320;
            b.push(m, a);
          }
          if (c + 1 == e || 16384 < b.length)
            (g += k.apply(null, b)), (b.length = 0);
        }
        return g;
      };
    f
      ? f(String, "fromCodePoint", {
          value: h,
          configurable: !0,
          writable: !0,
        })
      : (String.fromCodePoint = h);
  })();
