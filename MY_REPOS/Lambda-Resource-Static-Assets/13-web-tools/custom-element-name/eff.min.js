"use strict";
((b, e) => {
  function h(a) {
    return encodeURIComponent(a).replace(
      /['()_*]/g,
      (c) => "%" + c.charCodeAt().toString(16)
    );
  }

  function k() {
    const a = d.value;
    const c = validate(a);
    c.isValid
      ? (validate(a),
        (l.className = "show"),
        c.message
          ? ((d.className = "warning"),
            (f.innerHTML =
              "<em>However</em>, as a best practice, " + c.message),
            (f.className = "show"))
          : ((d.className = "valid"), (f.className = "hide")))
      : ((f.innerHTML = c.message),
        (f.className = "show"),
        (l.className = "hide"),
        (d.className = "invalid"));
    m.hash = h(a);
    g && (g.customElementName = a);
  }
  var d = e.getElementsByTagName("input")[0];
  var l = b.valid;
  var f = b.message;
  var m = e.getElementById("permalink");

  var g = (() => {
    const a = new Date();
    let c;
    try {
      (c = b.localStorage).setItem(a, a);
      const n = c.getItem(a) == a;
      c.removeItem(a);
      return n && c;
    } catch (p) {}
  })();

  d.onkeyup = k;
  d.oninput = () => {
    d.onkeyup = null;
    k();
  };
  g && (g.customElementName && (d.value = g.customElementName), k());
  b.onhashchange = () => {
    d.value = decodeURIComponent(location.hash.slice(1));
    k();
  };
  if (location.hash) b.onhashchange();
})(this, document);
window._gaq = [["_setAccount", "UA-6065217-60"], ["_trackPageview"]];
((b, e) => {
  const h = b.createElement(e);
  b = b.getElementsByTagName(e)[0];
  h.src = "https://www.google-analytics.com/ga.js";
  b.parentNode.insertBefore(h, b);
})(
       = encode(valu