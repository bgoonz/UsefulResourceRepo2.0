import React from 'react'
import DefaultLayout from '../../components/layout'

const Layout = ({ children }) => (
  <DefaultLayout title="Events - Meetup 🎉">{children}</DefaultLayout>
)

export default Layout
