import { FontWeightToken as DesignFontWeightToken } from "gatsby-design-tokens"
export { fontWeights as default } from "gatsby-design-tokens"

export type FontWeightToken = DesignFontWeightToken
/*

fontWeights = {
  body: 400,
  semiBold: 600,
  bold: 700,
  extraBold: 800,
  heading: 700,
}

*/
