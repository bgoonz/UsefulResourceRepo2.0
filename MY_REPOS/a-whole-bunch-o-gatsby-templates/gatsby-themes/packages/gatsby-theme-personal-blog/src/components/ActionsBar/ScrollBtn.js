import React from 'react';

import { FiArrowUp } from 'react-icons/fi';

import IconButton from './IconButton';

export default () => {
  return (
    <IconButton>
      <FiArrowUp />
    </IconButton>
  );
};
