function <name>(){
	echo "??????????????????"
	read repoName
	echo "??????????????????????/"
		echo
			options=("public" "private" )
			select_option "${options[@]}"
			choice=$?
		value=${options[$choice]}
		echo "$value"
		if [ $value = 'public' ]
			then 
				bool="false"
		fi
		if [ $value = 'private' ]
			then 
				bool="true"
		fi
}
