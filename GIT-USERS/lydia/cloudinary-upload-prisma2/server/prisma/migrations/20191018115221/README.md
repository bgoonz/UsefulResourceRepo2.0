# Migration `20191018115221`

This migration has been generated by Lydia Hallie at 10/18/2019, 11:52:21 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Image" (
  "format" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "publicId" TEXT NOT NULL DEFAULT ''  ,
  "version" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Image" ("format","id","publicId") SELECT "format","id","publicId" from "Image"

DROP TABLE "lift"."Image";

ALTER TABLE "lift"."new_Image" RENAME TO "Image";

CREATE UNIQUE INDEX "lift"."Image.publicId" ON "Image"("publicId")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191018111840-init..20191018115221
--- datamodel.dml
+++ datamodel.dml
@@ -10,5 +10,6 @@
 model Image {
   id       String @default(cuid()) @id
   publicId String @unique
   format   String
+  version  String
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191018115221)
in your `before` or `after` migration script like this:

```ts
import Photon from "@generated/photon/20191018115221";

const photon = new Photon();

async function main() {
  const result = await photon.users();
  console.dir(result, { depth: null });
}

main();
```
