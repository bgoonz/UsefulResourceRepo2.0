# Migration `20191018111840-init`

This migration has been generated by Lydia Hallie at 10/18/2019, 11:18:40 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Image" (
  "format" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "publicId" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX "lift"."Image.publicId" ON "Image"("publicId")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191018111840-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,14 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model Image {
+  id       String @default(cuid()) @id
+  publicId String @unique
+  format   String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191018111840-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191018111840-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
