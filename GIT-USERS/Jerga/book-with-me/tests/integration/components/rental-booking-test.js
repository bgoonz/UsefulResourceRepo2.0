import { moduleForComponent, test } from "ember-qunit";

moduleForComponent(
  "rental-booking",
  "Integration | Component | rental booking",
  {
    integration: true,
  }
);

test("it renders -PASS-", function (assert) {
  assert.ok(true);
});
