import React from 'react';
import PropTypes from 'prop-types';
import VisibleTodolist from '../containers/VisibleTodolist';
import { Section } from 'react-bulma-components/full';
const Main = ({ todos }) => <VisibleTodolist />;

export default Main;
